# J-Bridge 실제 AI 연결

## 처음 한 번
1. 이 폴더의 `.env` 파일을 메모장이나 코드 편집기로 엽니다.
2. `OPENAI_API_KEY=`의 등호 뒤에 본인의 API 키를 붙이고 저장합니다. 키를 채팅에 보내지 마세요.
3. `OPENAI_MODEL=gpt-4.1-mini`는 그대로 둡니다.

## 실행
1. `start.cmd`를 더블 클릭합니다. Node.js 22 이상이 필요합니다. 현재 작업 컴퓨터에는 Node.js가 있습니다.
2. 열린 창을 유지하고 브라우저에서 http://127.0.0.1:8787 을 엽니다.
3. 샘플 메일 하나를 선택하고 AI 분석하기를 누릅니다. 실제 OpenAI 요청이 발생하며 API 사용량에 따라 과금될 수 있습니다.
4. 결과를 검토하고 일본어 초안을 수정한 뒤 답장만 복사를 누릅니다.
5. 사용을 끝내면 실행 창에서 Ctrl+C를 누릅니다.

기존 HTML 파일을 더블 클릭해서 여는 대신 위 주소를 이용하세요. 키 설정 변경은 저장 즉시 다음 분석에 반영됩니다. 'API 키 설정됨'은 키가 입력되었다는 뜻이며 유효성/잔액은 실제 분석 시 확인됩니다.

## 현재 범위
- 다운로드해 준 새 디자인을 바탕으로 만든 별도의 AI 연결 버전입니다.
- 본문만 OpenAI Responses API로 전송합니다. 이 앱은 메일 본문과 결과를 파일에 저장하지 않습니다. API 요청에는 store:false를 사용하지만 이것이 OpenAI 측의 모든 데이터 보관을 없앤다는 뜻은 아닙니다.
- 앱은 이 컴퓨터에서만 접속할 수 있습니다. 공개 배포와 Gmail 연동은 아직 하지 않았습니다.
- 한 번에 한 건, 시간당 최대 20회 요청합니다. 서버 재시작 시 횟수는 초기화되므로 절대적인 비용 한도가 아닙니다.
- 초안을 수정하면 한국어 뜻은 수정 전 기준입니다.
- `.env`는 비밀 파일입니다. 폴더를 공유하거나 배포할 때 제외하세요. 웹 서버는 이 파일을 제공하지 않습니다.

## 검증 범위
가짜 응답을 이용한 연결 로직, 입력 오류, 키 미설정, 비밀 파일 접근 차단을 검사했습니다. 실제 API 키로 유료 분석을 완료한 것은 아닙니다.

공식 문서: https://developers.openai.com/api/docs/quickstart
